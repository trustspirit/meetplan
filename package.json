{
  "name": "meetplan",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "pnpm --filter @meetplan/shared build && pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "emulators": "pnpm --filter @meetplan/shared build && pnpm --filter functions build && firebase emulators:start",
    "deploy": "pnpm --filter @meetplan/shared build && pnpm --filter web build && pnpm --filter functions build && firebase deploy"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
